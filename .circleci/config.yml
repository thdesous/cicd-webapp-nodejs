version: 2.1
jobs:
  test:
    working_directory: ~/app
    docker:
      - image: cimg/node:15.1
    steps:
      - checkout
      - run:
          name: Install packages
          command: |
            ls -al
            pwd
            cd /app
            ls -al
            npm install
      - run:
          name: Run App
          command: |
            ls -al
            pwd
            cd /app
            ls -al
            cat package.json

workflows:
  my-workflow:
    jobs:
      - test